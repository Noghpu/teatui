---
id: 00012-2026-05-29-1670ea75-pane-local-keymap
created_at: 2026-05-29T22:38:22+02:00
created_by_model: claude-opus-4-7/high
state: implemented
state_updated_at: 2026-05-29T22:46:32+02:00
---
# Pane-local key dispatch on Generate screen

## Goal
Restructure the Generate-screen normal-mode key handling so each pane (Menu, Form, Preview) owns its action keys. Pane-specific shortcuts (`g`, `c`, `p`, `i`, `r`) must no-op when their owning pane is not focused. Truly global keys (`Tab`, `Shift+Tab`, `Esc`, `q`/`Ctrl+C`) keep working everywhere.

## Context
Today `apply_normal_key` in `src/app.rs` maps keys globally on the Generate screen (around `src/app.rs:558-566`). The user reports the keymap feels broken because the same key has different meanings in different panes and the footer hints do not reflect where each key actually fires. Making each pane a small modal scope is the first foundational step for the subsequent footer hint, force-sync, head-field, blocker banner, and spinner tickets. Background: see `docs/design.md` for the three-pane Generate layout.

## Non-Goals
- Rewriting the footer hint line â€” that lives in a separate ticket (`pane-aware-footer-hints`).
- Changing what `g` / `c` / `p` actually do once they fire. Only the dispatch gating changes here.
- Touching `InputMode::Editing` / `InputMode::Confirm` handling â€” those already have dedicated arms.
- Adding new actions or new focus targets.

## Design Decisions
- Pane-local map for Generate / Normal:
  - Menu: arrows / `j` / `k` move selection, `Enter` picks the revset (existing `select_revset`), `r` refreshes revsets.
  - Form: arrows / `j` / `k` move field, `Enter` and `i` begin edit, `g` runs `generate_pr`.
  - Preview: arrows / `j` / `k` scroll, `p` toggles prompt view, `g` regenerates, `c` confirms execution.
- Truly global on Generate / Normal: `Tab`, `Shift+Tab`, `Esc`, `q`, `Ctrl+C`.
- Implementation shape: keep a single `apply_normal_key` entry point but match on `(self.screen, self.focus, key.code)`. Out-of-pane keys return `Action::Tick` (current pattern for unhandled keys) so they are silently ignored.
- `generate_pr` and `confirm_execution` keep their existing screen/phase guards; we are only changing the *dispatch* layer, not the handlers' internal checks.
- Do not introduce a new enum for pane-modes; the existing `Focus` enum is the mode.

## Implementation Plan
1. In `src/app.rs`, restructure the Generate-screen normal-mode branch of `apply_normal_key` (around `src/app.rs:558-566`):
   - Keep the global pre-checks (Esc/Tab/Shift+Tab/q/Ctrl+C) above the pane-local match.
   - Add a `match self.focus` switch that produces the correct `Action` for `g`, `c`, `p`, `i`, `r`, and `Enter`.
   - Arrow / `j` / `k` already route through `Direction::Up`/`Down` â†’ `navigate` which is focus-aware; keep that.
2. Audit other Generate-screen handlers (`Action::Generate`, `Action::Confirm`, `Action::TogglePromptView`, `Action::Refresh`) to make sure no callers assume cross-pane firing. Add a focus guard inside each handler only if dispatch alone is not enough (prefer dispatch).
3. Update the existing app-level tests in `src/app.rs` that send `g` / `c` / `p` / `i` / `r` keys without setting `focus`. Set `app.focus` explicitly to the owning pane before pressing. Affected tests include around lines 1717, 1756, 1837, 1904, 2121, 2161, 2175, 2208 (these may have shifted; search for `KeyCode::Char('g')`, `KeyCode::Char('c')`, and `KeyCode::Char('p')`).
4. Add one new focused test: pressing `p` while `focus == Focus::Menu` results in `Action::Tick` (or equivalent no-op) and does not toggle `prompt_view`.

## Agent Handoff

<!-- ticket-section:agent-handoff v1 -->
```json
{
  "read_next": ["AGENTS.md", "docs/design.md", "src/app.rs", "src/action.rs"],
  "likely_files": ["src/app.rs", "src/action.rs"],
  "verification_commands": ["just fmt", "just check", "just clippy", "just test", "just verify"],
  "review_focus": [
    "All g/c/p/i/r presses outside their owning pane are silent no-ops.",
    "Tab/Shift+Tab/Esc/q/Ctrl+C still work from every Generate pane.",
    "No regression in InputMode::Editing or InputMode::Confirm handling.",
    "Existing tests updated to set focus before pressing pane-local keys."
  ],
  "jj_description_prefix": "feat"
}
```

## Acceptance Criteria
- On the Generate screen in `InputMode::Normal`:
  - `g` only triggers `Action::Generate` when `focus == Focus::Form` or `focus == Focus::Preview`.
  - `c` only triggers confirm when `focus == Focus::Preview`.
  - `p` only toggles prompt view when `focus == Focus::Preview`.
  - `i` only begins editing when `focus == Focus::Form`.
  - `r` only refreshes when `focus == Focus::Menu`.
  - `Enter` picks revset on Menu and begins edit on Form (existing behavior, now pane-local).
- Tab/Shift+Tab/Esc/q/Ctrl+C continue to work from all three Generate panes.
- All existing tests pass after focus has been set explicitly where required.
- New test confirms `p` is a no-op outside Preview focus.

## Verification Plan
- `just verify` (covers fmt, check, clippy, test).
- Manual smoke: launch the TUI in a workspace, navigate to Generate, confirm pane-local key behavior across all three panes.

## Files Likely Touched
- `src/app.rs`
- `src/action.rs` (only if a new no-op action helps clarity; prefer reusing `Action::Tick`)

## Risks
- Existing tests rely on focus state that was implicit; missing one update will surface as a flipped action assertion.
- Macro-style `match` over `(screen, focus, code)` can become unwieldy â€” keep it readable by extracting a small `fn dispatch_generate_normal(...)` helper if the match grows.
- `Enter` already does two things (pick revset on Menu, begin edit on Form). Make sure the dispatch keeps both, not just one.
---

<!-- ticket-section:implementation-note v1 -->
## Implementation Note

Metadata:
- model: claude-sonnet-4-6
- completed_at: 2026-05-29T22:46:32+02:00
- state: implemented

## What was completed

Restructured the Generate-screen normal-mode key dispatch in `src/app.rs` so that pane-specific shortcuts (`g`, `c`, `p`, `i`, `r`) are silently ignored when their owning pane is not focused. Truly global keys (`Tab`, `Shift+Tab`, `Esc`, `q`, `Ctrl+C`, arrows, `j`/`k`, `h`/`l`, `Enter`) continue to work from all three panes.

## Key changes

- Added a free function `dispatch_generate_normal(focus: Focus, key: KeyEvent) -> Option<Action>` that encodes the pane-local keymap as a `match (focus, key.code)` table. Returns `None` for global keys (falls through to existing global match).
- Inserted a call to `dispatch_generate_normal` in `handle_key` when `screen == Screen::Generate`, placed after the phase guards (CheckingFreshness / Confirming / Executing) and before the global match.
- Gated the DraftReady/Failed `'c' => ConfirmExecution` arms on `self.focus == Focus::Preview` using Clippy-preferred match guards.
- Removed no-op (`Action::Tick`) arms for out-of-pane keys from the global match (they were already Tick, but now the pane-local dispatch returns Tick explicitly before reaching the global match).

## Deviations from plan

None. Implementation follows the design exactly: single entry point, `dispatch_generate_normal` helper for readability, `Focus` enum as the mode.

## Tests updated

- `draft_ready_maps_c_to_confirm_execution`: added `app.focus = Focus::Preview`
- `failed_phase_maps_c_to_confirm_execution`: added `app.focus = Focus::Preview`
- `generate_pr_c_confirm_behavior_not_regressed`: added `app.focus = Focus::Preview` for both DraftReady and Failed sub-checks

## New tests added

- `p_is_noop_outside_preview_focus` â€” required by ticket; confirms `p` returns Tick from Menu and Form
- `p_toggles_prompt_view_on_preview_focus`
- `g_fires_generate_only_on_form_and_preview`
- `r_fires_refresh_only_on_menu`
- `i_fires_edit_only_on_form`
- `global_keys_work_from_all_generate_panes` â€” Tab/Esc/q work from all three panes
- `c_is_noop_in_generate_normal_outside_preview` â€” 'c' is Tick on Menu/Form in SelectingRevset phase

## Verification

`just verify` passes: fmt, check, clippy (clean), test (208/208 pass).

## Risks / follow-up

- Footer hint ticket (`pane-aware-footer-hints`) should now use the same `dispatch_generate_normal` table as the single source of truth for what each pane shows.
- No internal handler guards were needed â€” dispatch gating alone is sufficient per the ticket design.
